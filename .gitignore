/env.json
/node_modules/
/.homeybuild/
/coverage/
*.bat
/.idea/
.DS_Store
# Emulator local config (holds the OpenAI API key)
/emulator/settings.json

# Matrix-runner local config (can hold API keys) and its TTS output clips
/emulator/matrix.json
/emulator/matrix-out/

# Emulator voice clips for the `mic` command (usually personal recordings)
/emulator/recordings/*.flac
/emulator/recordings/*.wav
/docs/music-assistant/music-assistant-data/

# Spike engine downloads (Needle WASM + weights, fetched by fetch-engine.mjs)
/spikes/*/engine*/
